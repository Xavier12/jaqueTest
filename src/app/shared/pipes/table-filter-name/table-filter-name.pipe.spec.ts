import { TableFilterNamePipe } from './table-filter-name.pipe';

describe('TableFilterNamePipe', () => {
  it('create an instance', () => {
    const pipe = new TableFilterNamePipe();
    expect(pipe).toBeTruthy();
  });
});
