import { TableFilterOrderNamePipe } from './table-filter-order-name.pipe';

describe('TableFilterOrderNamePipe', () => {
  it('create an instance', () => {
    const pipe = new TableFilterOrderNamePipe();
    expect(pipe).toBeTruthy();
  });
});
