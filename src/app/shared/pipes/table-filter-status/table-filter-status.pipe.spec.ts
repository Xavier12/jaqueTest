import { TableFilterStatusPipe } from './table-filter-status.pipe';

describe('TableFilterStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new TableFilterStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
